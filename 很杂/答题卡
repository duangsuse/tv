试卷是试题<ol>构成，Ctrl I 创建<u>下划线字段
导出为元组列表，其[0]是答案，1是题面；方便发卷时分割掉答案、批改时重组。

若含',' 则是单选题，其[0] 是正确答案，其余用于随挑迷惑；否则是填空题，添加1空，可继续
若含",,"则是多选题，其前是正确答案，打乱后保留正确答案序号。

第二<ol>是判断题正确项'Y'；第三<ol>则是错误'N'；题面[0]=='?'


变为元组后，乱序选前60 个这样组卷，让UI生成 选择/填空答题纸，批改求总分百分比

题面"__"被改为 input,select 或抽离到题面下方变<u> ；若多空就编号
其答案由 getAnswer(div) 在下一题或总览修改时填入答题数组，最终结合原答案批改。
wrapIf(用卡,<input>,append;<u>number) 实现抽离替换，有 TabNext(fun done(e0){}) 渲染UI并在切换时(存答案|null)
