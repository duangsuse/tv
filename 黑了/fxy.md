a b pair
c libc,ctx,char
d-distance l-length
e element,item,entry
f func fp file/path
g graph,draw
h w y x: 2D
i j n m: sheet,list,matrix
k key,id,scale
o obj,any op func
p test,predicate,point
q question,isXX
s str,seq,socket
t dict,translate,text
u url,unixRes
v hold,value
a-ary b-bin x-item
r z: unused, "xz" names a stream if need

v0 "V", v1: old&new
e "E", ee: inner item

sakx.as skax.as copy&group
axf.ax map(View compose)

Concat obj(&objs in it) to one serialized chunk(e.g. JSON)
Cut user read/input-able cat() str to vals, to calculate. may validate&fail, so it's "forwards" step

vwaxq prbd cstefg ijklm (nouse)hjnouyz
vw image/audio; ax,q  array(float), bool
pr point/rect; bd color/angle-direction
cst str,preset(with add-clip), time
fx_x,eg fnarg(x.x), ease-fn, svgPath
ijklm int,vec,%,length,mat

x.x mathFn
t.p parameterized
t.l pol
p.q implicit

lavfi gmic frei0r gimp magick natro tess
ladspa rub synfx chipp vosk

2e s,c,t,rad hyp=$N
xx.x pow; xxk.x mix
e abs,length,i?sqrt,exp2?,log2?,sign,mod, normalize
vp.b texture; v.p dim
xx.e clamp,smooth?step
4e trunc,floor,round,ceil
2fx_xx min,max

pp.x distance; jj.x dot
3j3j.3j cross; e inverse,transpose
x.q isNan,isInf
aq.q all,any,not

时轴：网格(拆分 重分,重录,动画点)、映射
合成：三分(画框,剪纸,擦拭,融二)、边框、堆积
